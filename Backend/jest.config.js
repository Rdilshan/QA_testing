module.exports = {
    testEnvironment: 'node',
    testTimeout: 10000, // Increase the timeout if necessary
    testRegex: './selenium/.*\\.js$', // Look for test files in the selenium directory
  };
  